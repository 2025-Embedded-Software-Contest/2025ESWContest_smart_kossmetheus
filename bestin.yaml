# Bestin 컴포넌트별 타임아웃 조정
# 이 설정은 custom_components/bestin/에서 사용됩니다

timeout: 10
reconnect_interval: 30
max_reconnect_attempts: 5

# SmartThings를 위한 HTTPS 설정 (필요 시 주석 해제)
# ssl_certificate: /ssl/fullchain.pem
# ssl_key: /ssl/privkey.pem
